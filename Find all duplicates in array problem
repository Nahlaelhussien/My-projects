class Solution:
    def findDuplicates(self, nums: List[int]) -> List[int]:
        count=Counter(nums)
        result=[]
        for key in count:
            if count[key]>1:
                result.append(key)
        return result
