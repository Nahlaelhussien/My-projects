class Solution:
    def findTheWinner(self, n: int, k: int) -> int:
        friends = list(range(1, n + 1))
        c=0
        while len(friends)>1:
            c = (c + k - 1) % len(friends)
            print (c)
            friends.pop(c)
        return friends[0]
